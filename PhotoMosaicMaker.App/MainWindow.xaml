<Window x:Class="PhotoMosaicMaker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoMosaicMaker.App"
        mc:Ignorable="d"
        Title="PhotoMosaicMaker" Width="1100" Height="800">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical">

            <TextBlock FontSize="16" FontWeight="Bold" Text="Inputs" Margin="0,0,0,8"/>

            <!-- Target -->
            <TextBlock Text="Target Image"/>
            <Grid Margin="0,4,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="90"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding TargetPath, Mode=OneWay}" />
                <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseTargetCommand}" />
            </Grid>

            <!-- Output Folder -->
            <TextBlock Text="Output Folder"/>
            <Grid Margin="0,4,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding OutputFolder, Mode=OneWay}" />
                <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseOutputFolderCommand}" />
                <Button Grid.Column="4" Content="Open" Command="{Binding OpenOutputFolderCommand}" />
            </Grid>

            <!-- Output file name -->
            <TextBlock Text="Output File Name (.png)"/>
            <TextBox Text="{Binding OutputFileNameText, UpdateSourceTrigger=PropertyChanged}"/>

            <Separator Margin="0,10"/>

            <!-- Video Sources (File + URL unified) -->
            <TextBlock FontSize="16" FontWeight="Bold" Text="Video Sources" Margin="0,0,0,8"/>

            <ListBox ItemsSource="{Binding VideoSources}"
                     SelectedItem="{Binding SelectedVideoSource, Mode=TwoWay}"
                     DisplayMemberPath="DisplayText"
                     Height="100"
                     Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Add Files..." Command="{Binding AddVideoFilesCommand}" Width="110" Margin="0,0,8,0"/>
                <Button Content="Remove" Command="{Binding RemoveSelectedVideoSourceCommand}" Width="80" Margin="0,0,8,0"/>
                <Button Content="Clear" Command="{Binding ClearVideoSourcesCommand}" Width="70"/>
            </StackPanel>

            <TextBlock Text="Add URL"/>
            <Grid Margin="0,4,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding VideoUrlInputText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2"
                        Content="Add"
                        Command="{Binding AddVideoUrlCommand}"/>
            </Grid>

            <Expander Header="Advanced: Downloader (yt-dlp)" IsExpanded="False" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="yt-dlp path (비우면 PATH의 yt-dlp 사용)"/>
                    <Grid Margin="0,4,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="90"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding YtDlpPath, Mode=OneWay}" />
                        <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseYtDlpCommand}" />
                    </Grid>

                    <CheckBox Content="No playlist"
                  IsChecked="{Binding YtDlpNoPlaylist, Mode=TwoWay}"
                  Margin="0,0,0,6"/>

                    <TextBlock Text="Max resolution (0 = unlimited)"/>
                    <TextBox Text="{Binding YtDlpMaxResolutionText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,4,0,0"/>
                </StackPanel>
            </Expander>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Frames per second (FPS)"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ExtractFpsText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Max frames per source (0=∞)"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ExtractMaxFramesText, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Button Content="Extract Frames to Sources" Height="32" Command="{Binding ExtractFramesCommand}" />

            <Separator Margin="0, 10"/>

            <!-- Mosaic settings -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="output size" Margin="0,0,0,6"/>
                <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
                    <RadioButton Content="HD (720p) " GroupName="OutputResolution" IsChecked="{Binding IsOutputHd, Mode=TwoWay}" />
                    <RadioButton Content="FHD (1080p) " GroupName="OutputResolution" IsChecked="{Binding IsOutputFhd, Mode=TwoWay}" />
                    <RadioButton Content="QHD (1440p) " GroupName="OutputResolution" IsChecked="{Binding IsOutputQhd, Mode=TwoWay}" />
                    <RadioButton Content="UHD (2160p) " GroupName="OutputResolution" IsChecked="{Binding IsOutputUhd, Mode=TwoWay}" />
                </StackPanel>

                <TextBlock Grid.Column="0" Grid.Row="1" Text="Tile Size"/>
                <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding TileSizeText, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            <CheckBox Content="소스 이미지를 잘라 사용 (성능이 저하될수 있음)"
                      IsChecked="{Binding UseSourcePatches, Mode=TwoWay}"
                      Margin="0,0,0,12"/>

            <TextBlock Text="Status"/>
            <TextBlock Text="{Binding StatusText}" Margin="0,4,0,12" TextWrapping="Wrap"/>

            <ProgressBar Height="18" Minimum="0" Maximum="1" Value="{Binding ProgressValue, Mode=OneWay}" Margin="0,0,0,12"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Build Library" Command="{Binding BuildLibraryCommand}" Width="110" Margin="0,0,8,0"/>
                <Button Content="Render" Command="{Binding RenderCommand}" Width="90" Margin="0,0,8,0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>

        <!-- Preview -->
        <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image Source="{Binding PreviewImage}" Stretch="Uniform" StretchDirection="Both"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
